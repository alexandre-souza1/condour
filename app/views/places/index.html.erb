  <div class="banner-cond" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= @condominium.photos.first.url %>);">
  </div>

  <div class="container d-flex">
   <div class="menu">
    <div class="card-cond p-2"> <!--Aqui inicia o cartão de Boas Vindas ao condomínio -->
     <h4>Bem-vindo ao,</h4>
     <h2 class=""><%= @condominium.name %>!</h2>
    </div>
    <div class="card-btn p-3 d-flex flex-column justify-content-between mt-1"> <!--Aqui iniciam os botões laterais -->
       <%= link_to condominium_path(@condominium), title: "Home" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Home</button>
      <% end %>
       <%= link_to condominium_path(@condominium), title: "Comunidade" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Comunidade</button>
      <% end %>
      <%= link_to condominium_services_path(@condominium), title: "Agendamento de Manutenção" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Manutenção</button>
      <% end %>
      <%= link_to condominium_places_path(@condominium), title: "Reservas de area comum" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Reservas</button>
      <% end %>
      <%= link_to condominium_path(@condominium), title: "Portaria`" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Portaria</button>
      <% end %>
      <%= link_to condominium_path(@condominium), title: "Regras do Condominio" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Regras do Condominio</button>
      <% end %>
      <%= link_to condominium_path(@condominium), title: "Apartamentos para alugar" do %>
        <button class="d-flex btn btn-primary text-white font-weight-bold text-decoration-none w-100 p-2">Apartamentos para Alugar</button>
      <% end %>
    </div>
   </div>

<div class="container card-home-condos p-3">
  <div class="row d-flex justify-content-between">
  <div class="col-6">
    <h2>Areas disponíveis:</h2>
  </div>
  <div class="col-4 d-flex justify-content-end">
    <%= link_to new_condominium_place_path, title: "Minhas Compras" do %>
      <button class="btn btn-primary text-white">Adicionar Area</button>
    <% end %>
  </div>
  </div>
  <div class="row d-flex justify-content-between">
  <div class="col-12">
  <div class="cards-trip">
    <% @places.each do |place| %>
        <div class="card-trip">
          <img src="<%= place.photos.first.url %>" />
          <div class="card-trip-infos">
          <%= link_to place_path(place), class: "text-decoration-none" do %>
            <div>
              <h2><%= place.name %></h2>
              <p><%= truncate(place.description, length: 35, omission: '...') %></p>
            </div>
          <% end %>
          </div>
        </div>
    <% end %>
  </div>
</div>
</div>
  </div>
</div>
